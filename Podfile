# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FinalGroupProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FinalGroupProject

pod 'Firebase/Analytics'
pod 'Firebase/Auth'
pod 'Firebase/Firestore'
pod 'Firebase/Core'
pod 'Firebase/Database'

  target 'FinalGroupProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalGroupProjectUITests' do
    # Pods for testing
  end

end
